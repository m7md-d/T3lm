/**
 * الأثر والتوقّعات — ميكانيزم العدّة، بمفتاحٍ خاصٍّ بهذا المنهج.
 * لا نقاط ولا شارات ولا سلاسل (الأسلوب §١١)، ومحلّيٌّ بلا حسابٍ وبلا إرسال.
 */
import { makeStore } from '@t3lm/kit/progress';

export const store = makeStore('t3lm-docker');
