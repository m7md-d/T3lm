/* المرحلة 01x — تجربةُ الفشل: ماذا يعني «عنوانُ عودةٍ صفر»؟
   بروتوكول Limine يدفع 0 على المكدَّس قبل القفز إلى نقطة الدخول.
   فالعودة من نقطة الدخول قفزٌ إلى العنوان 0 — وهنا نفعلها صريحةً. */
#include "kernel.h"

void stage_main(void) {
    void (*nowhere)(void) = (void (*)(void))0;
    nowhere();                       /* لا IDT بعد: الخطأ لا يجد معالجاً */
}
