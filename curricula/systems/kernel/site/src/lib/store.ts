/**
 * الأثر — `@t3lm/kit/progress` بمفتاح هذا المنهج.
 *
 * ولا يُحفَظ إلا ما يُعرَض: اللقطات المعبورة، وما كتبه القارئ في
 * `Checkpoint` وقبل فتح المصدر. ولا نقاط ولا سلاسل ولا عدّاد أيام.
 */
import { makeStore } from '@t3lm/kit/progress';

export const store = makeStore('t3lm:kernel');
