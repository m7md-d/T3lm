/**
 * سجلّ المكوّنات التفاعلية — **يُحقَن عند القسم الذي أنتج ادّعاءه**، لا في
 * صفحةٍ منفصلة. وفحصٌ في `../../scripts/ssr-check.tsx` يفرض أن يطابق كلٌّ منها
 * **قسماً واحداً بالضبط** — لا صفر ولا اثنين، فلا ينجرف عند أوّل تعديل.
 */
export type Lab = {
  id: string;
  region: string;
  /** مقطعٌ من عنوان القسم الذي يلي المختبرُ نصَّه. */
  after: string;
  claim: string;
};

export const labs: Lab[] = [
  {
    id: 'binding',
    region: '01',
    after: 'ما الذي تفعله',
    claim: '`=` تربط اسماً بكائن، والطريقة تعدّل الكائن',
  },
];
