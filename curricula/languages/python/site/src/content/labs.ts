/**
 * سجلّ المكوّنات التفاعلية — **يُحقَن عند الخطوة التي أنتجت ادّعاءه**، لا في
 * صفحةٍ منفصلة. وفحصٌ في `../../scripts/ssr-check.tsx` يفرض أن يطابق كلٌّ منها
 * **لقطةً واحدة بالضبط** — لا صفر ولا اثنتين، فلا ينجرف عن موضعه عند أوّل تعديل.
 */
export type Lab = {
  id: string;
  region: string;
  /** مقطعٌ من عنوان اللقطة التي يلي المختبرُ نصَّها. */
  after: string;
  claim: string;
};

export const labs: Lab[] = [
  {
    id: 'binding',
    region: '02',
    after: 'الاسم ليس خانة',
    claim: '`=` تربط، والطريقة تعدّل',
  },
];
