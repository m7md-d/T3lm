/**
 * رسمٌ نصّيّ — ثمانيةَ عشرَ منها في المصدر: تخطيطُ بتات، وشجرةُ أنواع، وطابورا
 * حلقة الأحداث. تُكتَب سياجاً بلا لغةٍ بعد `<!-- part -->`، فيميّزها التصريف.
 *
 * **واتّجاهها LTR** كما تعرضها أدوات التحليل، ويحاذيها نصٌّ عربيٌّ شارحٌ حولها.
 * ولا زرَّ نسخٍ ولا وسمَ ملفّ: ليست كوداً يُشغَّل.
 */
export function Figure({ text }: { text: string }) {
  return (
    <figure className="figure">
      <pre className="figure__body en">{text}</pre>
    </figure>
  );
}
