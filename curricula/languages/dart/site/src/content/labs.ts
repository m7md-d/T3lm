/**
 * سجلّ المختبرات — **يُحقَن كلٌّ بعد لقطته بالضبط**، لا في صفحةٍ مجمَّعة.
 *
 * و`after` مقطعٌ من عنوان اللقطة في الماركداون، ويفرض `../../scripts/ssr-check.tsx`
 * أن يطابق **واحدةً بالضبط** — لا صفر ولا اثنتين. وبلا هذا الفحص تنجرف
 * المختبرات عن مواضعها بصمت عند أوّل تعديلٍ في النصّ.
 *
 * واثنان فقط، ولكلٍّ **حالةٌ مفاجئة**: مختبرٌ بلا واحدةٍ لعبةٌ لا مختبر.
 */
import type { LabDef } from '../lib/types';

export const labs: LabDef[] = [
  {
    id: 'two-machines',
    region: '02',
    after: 'وعلى الويب ليست كذلك',
    name: 'آلتان، جوابان',
    claim: 'أعدادك الصحيحة آمنةٌ حتى 2^53 لا 2^63 إن كان برنامجك سيعمل على الويب',
  },
  {
    id: 'event-loop',
    region: '13',
    after: 'ولهذا تُجَوَّع الحلقة',
    name: 'الطابوران، خطوةً خطوة',
    claim: 'لو لم يكن للعدّاد حدّ، لما وصل الحدث أبداً — والبرنامج حيٌّ لا معلّق',
  },
];

export const labFor = (region: string, title: string) =>
  labs.find((l) => l.region === region && title.includes(l.after));
